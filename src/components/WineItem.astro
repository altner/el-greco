---
interface Props {
  number?: string;
  name: string;
  allergens?: string;
  price: string;
  type?: string;
  character?: string;
  pairing?: string;
}
const { number, name, allergens, price, type, character, pairing } = Astro.props;
---

<div class="mb-4 last:mb-0 pb-4 last:pb-0 border-b last:border-b-0" style="border-color: var(--rule);">
  <div class="flex items-start justify-between gap-4">
    <div class="flex-1 min-w-0">
      <div class="flex items-baseline gap-2 flex-wrap">
        <span
          class="font-sans font-semibold text-[15px]"
          style="color: var(--ink);"
        >
          {number && <span class="mr-0.5">{number}.</span>}{name}
        </span>
        {allergens && (
          <span
            class="font-sans font-medium text-[9.5px] tracking-[0.12em] uppercase"
            style="color: var(--ink-mute);"
          >
            {allergens}
          </span>
        )}
      </div>
      {type && (
        <p class="font-serif italic text-[14px] mt-0.5 leading-snug" style="color: var(--ink-soft);">
          {type}
        </p>
      )}
      {character && (
        <p class="font-serif italic text-[13.5px] leading-snug" style="color: var(--ink-mute);">
          {character}
        </p>
      )}
      {pairing && (
        <p class="font-serif italic text-[13.5px] leading-snug" style="color: var(--ink-mute);">
          {pairing}
        </p>
      )}
    </div>
    <span
      class="font-sans font-medium text-[15px] whitespace-nowrap text-right flex-none"
      style="color: var(--ink); font-variant-numeric: tabular-nums; min-width: 7rem;"
    >
      {price}
    </span>
  </div>
</div>
